[package]
name = "migration"
version.workspace = true
edition.workspace = true
publish.workspace = true
license-file.workspace = true

[lib]
name = "migration"
path = "src/lib.rs"

[dependencies]
tokio = { version = "1.22.0", features = ["rt", "macros"] }
sea-orm-migration = { version = "0.10.4", default-features = false, features = ["sqlx-postgres", "runtime-tokio-rustls", "cli"] }
entity = { path = "../entity" }
const_format = "0.2.30"
