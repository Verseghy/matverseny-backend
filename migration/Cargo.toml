[package]
name = "migration"
version.workspace = true
edition.workspace = true
publish.workspace = true

[lib]
name = "migration"
path = "src/lib.rs"

[dependencies]
tokio = { version = "1.21.1", features = ["rt", "macros"] }
sea-orm-migration = { version = "0.9.2", default-features = false, features = ["sqlx-postgres", "runtime-tokio-rustls"] }
entity = { path = "../entity" }
